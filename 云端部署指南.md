# 星汇 · 云端部署指南（电脑关机也能自动更新）

## 先说结论

要做到「电脑不开机，手机打开 App 也是最新的」，**必须有一个联网的机器帮你定时采集**——手机自己不行（手机浏览器不允许后台长期跑任务）。

好消息：采集脚本已经改成**纯 HTTP 模式**（`collector/collect_http.js`），不再需要浏览器、不再需要你电脑开机，任何能跑 Node 的地方都能执行。所以搬上云端是顺理成章的事。

---

## 三种方案对比

| 方案 | 费用 | 难度 | 隐私 | 说明 |
|---|---|---|---|---|
| **A. GitHub Actions + Pages** ★推荐 | 免费 | 中 | 页面公开（动态内容本身也是公开的） | 10 分钟采一次，手机访问免费域名 |
| **B. Cloudflare Workers + KV** | 免费 | 中高 | 私有 | 更快更稳，可设密码访问 |
| **C. 轻量云服务器** | 约 30~50 元/月 | 低 | 完全私有 | 把现在的 server.js 整个搬上去，最灵活 |

---

## 方案 A：GitHub Actions + Pages（免费，推荐先试）

### 第 1 步：创建仓库

1. 打开 github.com 注册/登录
2. 新建仓库（New repository），名字例如 `starhub`，**选 Public**（Pages 免费版要求公开）
3. 创建后先不要关页面

### 第 2 步：上传文件

把本机这个文件夹里的东西传上去：

```
C:\Users\lulu0\WorkBuddy\2026-09-23-19-40-28\starhub\
```

**要传**：`index.html`、`img\`、`data\posts.json`、`collector\collect_http.js`、`.github\`

**不要传**：`browser-profile\`（这是你的浏览器登录数据，绝对不能上传）、`collector\collect.js`、`server.js`、`*.bat`

> 最省事的做法：在仓库页面点 "Add file → Upload files"，把上面几个文件/文件夹拖进去。
> `.github` 开头的是隐藏文件夹，Windows 资源管理器里能直接看到，一起拖进去即可。

### 第 3 步：配置 Cookie（关键）

Cookie 已经帮你导出来了，在：

```
C:\Users\lulu0\WorkBuddy\2026-09-23-19-40-28\starhub\data\cookies.json
```

打开它，复制 `cookie` 后面那一整行字符串（很长，1125 个字符左右）。

然后回到 GitHub 仓库：
- Settings → Secrets and variables → Actions → **New repository secret**
- Name 填 `WEIBO_COOKIE`，Value 粘贴刚才那串
- （可选）再建一个叫 `SENDKEY` 的 secret，填 Server酱的 SendKey，用于有新动态时推送到微信

> **Cookie 绝对不要写进任何公开文件**。它只放在 GitHub Secrets 里，Actions 运行时注入，别人看不到。

### 第 4 步：开启 Pages

- Settings → Pages → Source 选 **Deploy from a branch**
- Branch 选 `main`，文件夹选 `/ (root)` → Save
- 等 1~2 分钟，页面会显示你的网址，形如 `https://你的用户名.github.io/starhub`

### 第 5 步：手机上使用

手机浏览器打开上面那个网址 → 菜单里选「添加到主屏幕」→ 桌面上就出现星汇图标。

以后**电脑不用开**，GitHub 每 10 分钟帮你采一次，手机打开就是最新的。

### 验证是否跑起来了

仓库页面 → **Actions** 标签 → 左侧点「星汇定时采集」→ 看有没有绿色的运行记录。也可以点右上角 "Run workflow" 手动触发一次。

---

## 关于微信推送（可选但很香）

不打开 App 也能知道他发了新内容：

1. 打开 sct.ftqq.com，微信扫码登录
2. 复制你的 SendKey
3. 填到仓库的 `SENDKEY` secret 里

之后每次采集发现新动态，会自动往你的微信推一条消息，点开就是原帖链接。

---

## 需要知道的限制（诚实说明）

1. **Cookie 会过期**：一般几周到几个月。过期后采集会失败（Actions 变红），此时在本机重新运行一次 `collector\export_cookies.js`，把新的 cookie 更新到 GitHub Secret 即可。整个过程 2 分钟。
2. **GitHub 定时有延迟**：`*/10` 不是严格每 10 分钟，高峰期可能延迟几分钟到十几分钟。想要更准时就用方案 C。
3. **Actions 会频繁 commit**：每 10 分钟提交一次，仓库历史会比较长。介意的话可以改成每 30 分钟（`cron: '*/30 * * * *'`）。
4. **页面公开**：任何人访问你的 Pages 网址都能看到这个聚合页。内容本身是微博公开动态，但如果介意，选方案 B 或 C。
5. **小红书 / 抖音尚未接入**：目前只自动采集微博，这两家反爬更强，后续再攻。

---

## 方案 C：轻量云服务器（最省心，要花钱）

如果你不想折腾 GitHub，也不想让页面公开：

1. 买一台轻量云服务器（腾讯云/阿里云轻量应用服务器，2 核 2G 约 30~50 元/月）
2. 装 Node，把整个 `starhub` 目录传上去
3. 运行 `node server.js`（用 `nohup` 或 systemd 守护）
4. 手机直接访问 `http://服务器公网IP:8787`

好处：完全私有、准时、还能继续用浏览器兜底模式（Cookie 过期时自动刷新登录态）。
